{
  "name": "ga-project3-group-budget-app",
  "version": "1.0.0",
  "description": "Budget App",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"nodemon server.js\" \"cd ./client  && npm start \" ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "10.0.0"
  },
  "author": "Annie T. Bellefleur",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^5.2.0",
    "express": "^4.17.3",
    "mongoose": "^5.9.15",
    "nodemon": "^2.0.4"
    
  }
}
